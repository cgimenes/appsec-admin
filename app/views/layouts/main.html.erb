<%= inside_layout "application" do %>
  <%= render 'layouts/navbar' %>

  <%= yield %>

  <div class="notifications">
    <% flash.each do |key, value| %>
      <%= render partial: 'shared/notification', locals: {type: key, message: value} %>
    <% end %>
  </div>
<% end %>
